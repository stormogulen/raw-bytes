name: Raw Bytes Container CI

on:
  push:
    paths:
      - "raw_bytes_container/**"
      - ".github/workflows/raw_bytes_container.yml"
  pull_request:
    paths:
      - "raw_bytes_container/**"

jobs:
  build:
    name: Build & Test raw_bytes_container
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: raw_bytes_container

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-rust@v2
        with:
          rust-version: stable

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build crate
        run: cargo build --all-targets

      - name: Build examples
        run: cargo build --examples

      - name: Run tests
        run: cargo test --all-targets

      - name: Run examples (if present)
        run: |
          if [ -d examples ]; then
            echo "Running examples..."
            for example in examples/*.rs; do
              exename=$(basename "${example%.rs}")
              cargo run --example "$exename" --quiet || exit 1
            done
          fi

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Lint with Clippy
        run: cargo clippy --all-targets -- -D warnings
