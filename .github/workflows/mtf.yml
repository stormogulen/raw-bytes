name: MFT CI

on:
  push:
    paths:
      - "mft/**"
      - ".github/workflows/mft.yml"
  pull_request:
    paths:
      - "mft/**"
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Run fmt check
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build crate and examples
        run: cargo build --package mtf --examples

      - name: Run tests
        run: cargo test --package mtf --all-features